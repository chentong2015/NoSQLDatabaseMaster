Relational Database benefits:
- 结构清晰，数据查询方便(例如JOIN不同表数据)
- 支持事务属性(ACID properties)
- 不容易Scale扩展，数据分片等问题

No-Relational Database benefits:
- 存储任意非结构化数据(JSON, Key-Value, XML ...)
- 高性能低延迟(Low-latency)
- 可以存储大量数据(Large amount of data)
- 但不支持事务属性(ACID properties)