在分布式锁场景下，在主从架构(哨兵架构)中如何解决锁的同步性?

问题场景：
如果redisson在redis主节点中加了一把锁(设置key)，从节点通过"主从复制"来完成锁的同步
如果key刚好设置到redis主结点，然后redis主结点挂了
此时哨兵模式会将其中一个还没有完成"主从复制"的从节点升级成主节点，导致新的主节点上没有key锁
此后新来的线程如果访问新的主结点，发现没有锁则会继续执行